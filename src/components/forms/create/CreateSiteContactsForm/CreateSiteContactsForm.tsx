import { useCreateSiteContacts } from "."

function CreateSiteContactsForm() {
  const methods = useCreateSiteContacts()

  return (
    <div>CreateSiteContactsForm</div>
  )
}

export default CreateSiteContactsForm